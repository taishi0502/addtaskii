
<div class="jumbotron">
 <div class="container-fluid">
  <div class="row">
    <div class="col-sm-6">
       <% unless user_signed_in? %>
          <h2 class="mypage-title"></span>addtaskiiへようこそ。 </h2>  
       <% else %>
          <h3 class="top">Be together</h3>
       <% end %>
     </div>
     <% if user_signed_in?%>
     <div class="missionset-btn col-sm-6">
       <%= link_to 'set a goal', new_mission_path, class: "setagoalbtn" %>
     </div>
     <% else %>
     <% end %>
    </div>
 </div>

<div class="container-fluid">
 <div class="row">
   <% unless user_signed_in? %>

   <div class="d-grid gap-2">
     <%= link_to "ログイン", new_user_session_path, class: "btn btn-primary" %>
     <%= link_to "登録がお済みでない方はこちら", new_user_registration_path, class: "btn btn-primary"%>
  </div>
   

   <% else %>
    

         <div class="row">
            <%= render partial: "mission", collection: @missions %>
         </div>
      </div>
     <% end %>
  </div>
 </div>
  

</div>

